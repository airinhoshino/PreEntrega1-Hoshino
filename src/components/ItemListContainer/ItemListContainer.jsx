//componente para mostrar el listado de items (más adelante)
const ItemListContainer = ({greeting}) => {
  return (
    <div style={{margin: '2rem'}}>{greeting}</div>
  )
}

export default ItemListContainer
